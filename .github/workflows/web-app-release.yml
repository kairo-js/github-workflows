name: Web App Release

on:
  workflow_call:
    inputs:
      app-name:
        required: true
        type: string
      image-prefix:
        required: true
        type: string
      services:
        description: JSON array passed to docker-build-push.yml.
        required: true
        type: string
      postgres-user:
        required: true
        type: string
      postgres-db:
        required: true
        type: string
      caddy-snippet-template-path:
        required: true
        type: string
      acme-email:
        required: true
        type: string
      prod-backup-prefix:
        required: true
        type: string
      prod-backup-gdrive-suffix:
        description: >
          Child folder under PROD_GDRIVE_DESTINATION for pre-prod-deploy backups.
          Defaults to "versions" because these dumps represent the data state
          before deploying a version tag.
        required: false
        type: string
        default: versions
      compose-path:
        required: false
        type: string
        default: deploy/docker-compose.yml
      dev-backend-upstream:
        required: false
        type: string
        default: ""
      dev-frontend-upstream:
        required: false
        type: string
        default: ""
      prod-backend-upstream:
        required: false
        type: string
        default: ""
      prod-frontend-upstream:
        required: false
        type: string
        default: ""
      dev-backend-port:
        required: false
        type: string
        default: ""
      dev-frontend-port:
        required: false
        type: string
        default: ""
      prod-backend-port:
        required: false
        type: string
        default: ""
      prod-frontend-port:
        required: false
        type: string
        default: ""
      dev-public-url:
        required: false
        type: string
        default: ""
      prod-public-url:
        required: false
        type: string
        default: ""
    secrets:
      DOCKERHUB_USERNAME:
        required: true
      DOCKERHUB_TOKEN:
        required: true
      DEV_DEPLOY_HOST:
        required: true
      DEV_DEPLOY_USER:
        required: true
      DEV_DEPLOY_SSH_KEY:
        required: true
      PROD_DEPLOY_HOST:
        required: true
      PROD_DEPLOY_USER:
        required: true
      PROD_DEPLOY_SSH_KEY:
        required: true
      DEV_POSTGRES_PASSWORD:
        required: true
      PROD_POSTGRES_PASSWORD:
        required: true
      PROXY_HOST:
        required: true
      PROXY_USER:
        required: true
      PROXY_SSH_KEY:
        required: true
      RCLONE_CONFIG:
        required: true
      PROD_GDRIVE_DESTINATION:
        required: true
      BASIC_AUTH_DEV_USER:
        required: false
      BASIC_AUTH_DEV_HASH:
        required: false
      BASIC_AUTH_ADMIN_USER:
        required: false
      BASIC_AUTH_ADMIN_HASH:
        required: false
      DEV_GOOGLE_CLIENT_ID:
        required: false
      DEV_GOOGLE_CLIENT_SECRET:
        required: false
      PROD_GOOGLE_CLIENT_ID:
        required: false
      PROD_GOOGLE_CLIENT_SECRET:
        required: false
      DEV_GITHUB_CLIENT_ID:
        required: false
      DEV_GITHUB_CLIENT_SECRET:
        required: false
      PROD_GITHUB_CLIENT_ID:
        required: false
      PROD_GITHUB_CLIENT_SECRET:
        required: false

permissions:
  contents: read

jobs:
  vars:
    runs-on: ubuntu-latest
    outputs:
      tag: ${{ steps.vars.outputs.tag }}
      deploy-env-name: ${{ steps.vars.outputs.deploy-env-name }}
    steps:
      - name: Determine image tag and deploy environment
        id: vars
        run: |
          if [ "${{ github.ref_type }}" = "tag" ]; then
            echo "tag=${{ github.ref_name }}" >> "$GITHUB_OUTPUT"
            echo "deploy-env-name=prod" >> "$GITHUB_OUTPUT"
          else
            echo "tag=dev" >> "$GITHUB_OUTPUT"
            echo "deploy-env-name=dev" >> "$GITHUB_OUTPUT"
          fi

  build-and-push:
    needs: vars
    uses: kairo-js/github-workflows/.github/workflows/docker-build-push.yml@v0.2.4
    with:
      services: ${{ inputs.services }}
      image-prefix: ${{ inputs.image-prefix }}
      tag: ${{ needs.vars.outputs.tag }}
    secrets:
      DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
      DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}

  pre-prod-backup:
    needs: [vars, build-and-push]
    if: ${{ needs.vars.outputs.deploy-env-name == 'prod' }}
    uses: kairo-js/github-workflows/.github/workflows/postgres-backup.yml@v0.2.4
    with:
      app-name: ${{ inputs.app-name }}
      deploy-env-name: prod
      compose-path: ${{ inputs.compose-path }}
      db-user: ${{ inputs.postgres-user }}
      db-name: ${{ inputs.postgres-db }}
      backup-prefix: ${{ inputs.prod-backup-prefix }}
      gdrive-destination-suffix: ${{ inputs.prod-backup-gdrive-suffix }}
      skip-if-missing: true
    secrets:
      DEPLOY_HOST: ${{ secrets.PROD_DEPLOY_HOST }}
      DEPLOY_USER: ${{ secrets.PROD_DEPLOY_USER }}
      DEPLOY_SSH_KEY: ${{ secrets.PROD_DEPLOY_SSH_KEY }}
      RCLONE_CONFIG: ${{ secrets.RCLONE_CONFIG }}
      BACKUP_GDRIVE_DESTINATION: ${{ secrets.PROD_GDRIVE_DESTINATION }}

  deploy-dev:
    needs: [vars, build-and-push]
    if: ${{ needs.vars.outputs.deploy-env-name == 'dev' }}
    uses: kairo-js/github-workflows/.github/workflows/app-deploy.yml@v0.2.4
    with:
      app-name: ${{ inputs.app-name }}
      image-prefix: ${{ inputs.image-prefix }}
      deploy-env-name: dev
      image-tag: ${{ needs.vars.outputs.tag }}
      app-env: dev
      public-url: ${{ inputs.dev-public-url }}
      backend-port: ${{ inputs.dev-backend-port }}
      frontend-port: ${{ inputs.dev-frontend-port }}
      postgres-user: ${{ inputs.postgres-user }}
      postgres-db: ${{ inputs.postgres-db }}
      compose-path: ${{ inputs.compose-path }}
    secrets:
      DEPLOY_HOST: ${{ secrets.DEV_DEPLOY_HOST }}
      DEPLOY_USER: ${{ secrets.DEV_DEPLOY_USER }}
      DEPLOY_SSH_KEY: ${{ secrets.DEV_DEPLOY_SSH_KEY }}
      DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
      POSTGRES_PASSWORD: ${{ secrets.DEV_POSTGRES_PASSWORD }}
      GOOGLE_CLIENT_ID: ${{ secrets.DEV_GOOGLE_CLIENT_ID }}
      GOOGLE_CLIENT_SECRET: ${{ secrets.DEV_GOOGLE_CLIENT_SECRET }}
      GITHUB_CLIENT_ID: ${{ secrets.DEV_GITHUB_CLIENT_ID }}
      GITHUB_CLIENT_SECRET: ${{ secrets.DEV_GITHUB_CLIENT_SECRET }}

  deploy-prod:
    needs: [vars, build-and-push, pre-prod-backup]
    if: ${{ needs.vars.outputs.deploy-env-name == 'prod' && needs.pre-prod-backup.result == 'success' }}
    uses: kairo-js/github-workflows/.github/workflows/app-deploy.yml@v0.2.4
    with:
      app-name: ${{ inputs.app-name }}
      image-prefix: ${{ inputs.image-prefix }}
      deploy-env-name: prod
      image-tag: ${{ needs.vars.outputs.tag }}
      app-env: prod
      public-url: ${{ inputs.prod-public-url }}
      backend-port: ${{ inputs.prod-backend-port }}
      frontend-port: ${{ inputs.prod-frontend-port }}
      postgres-user: ${{ inputs.postgres-user }}
      postgres-db: ${{ inputs.postgres-db }}
      compose-path: ${{ inputs.compose-path }}
    secrets:
      DEPLOY_HOST: ${{ secrets.PROD_DEPLOY_HOST }}
      DEPLOY_USER: ${{ secrets.PROD_DEPLOY_USER }}
      DEPLOY_SSH_KEY: ${{ secrets.PROD_DEPLOY_SSH_KEY }}
      DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
      POSTGRES_PASSWORD: ${{ secrets.PROD_POSTGRES_PASSWORD }}
      GOOGLE_CLIENT_ID: ${{ secrets.PROD_GOOGLE_CLIENT_ID }}
      GOOGLE_CLIENT_SECRET: ${{ secrets.PROD_GOOGLE_CLIENT_SECRET }}
      GITHUB_CLIENT_ID: ${{ secrets.PROD_GITHUB_CLIENT_ID }}
      GITHUB_CLIENT_SECRET: ${{ secrets.PROD_GITHUB_CLIENT_SECRET }}

  deploy-caddy-dev:
    needs: deploy-dev
    if: ${{ needs.deploy-dev.result == 'success' }}
    uses: kairo-js/github-workflows/.github/workflows/caddy-snippet-deploy.yml@v0.2.4
    with:
      app-name: ${{ inputs.app-name }}
      snippet-template-path: ${{ inputs.caddy-snippet-template-path }}
      acme-email: ${{ inputs.acme-email }}
      dev-backend-upstream: ${{ inputs.dev-backend-upstream }}
      dev-frontend-upstream: ${{ inputs.dev-frontend-upstream }}
      prod-backend-upstream: ${{ inputs.prod-backend-upstream }}
      prod-frontend-upstream: ${{ inputs.prod-frontend-upstream }}
    secrets:
      PROXY_HOST: ${{ secrets.PROXY_HOST }}
      PROXY_USER: ${{ secrets.PROXY_USER }}
      PROXY_SSH_KEY: ${{ secrets.PROXY_SSH_KEY }}
      BASIC_AUTH_DEV_USER: ${{ secrets.BASIC_AUTH_DEV_USER }}
      BASIC_AUTH_DEV_HASH: ${{ secrets.BASIC_AUTH_DEV_HASH }}
      BASIC_AUTH_ADMIN_USER: ${{ secrets.BASIC_AUTH_ADMIN_USER }}
      BASIC_AUTH_ADMIN_HASH: ${{ secrets.BASIC_AUTH_ADMIN_HASH }}

  deploy-caddy-prod:
    needs: deploy-prod
    if: ${{ needs.deploy-prod.result == 'success' }}
    uses: kairo-js/github-workflows/.github/workflows/caddy-snippet-deploy.yml@v0.2.4
    with:
      app-name: ${{ inputs.app-name }}
      snippet-template-path: ${{ inputs.caddy-snippet-template-path }}
      acme-email: ${{ inputs.acme-email }}
      dev-backend-upstream: ${{ inputs.dev-backend-upstream }}
      dev-frontend-upstream: ${{ inputs.dev-frontend-upstream }}
      prod-backend-upstream: ${{ inputs.prod-backend-upstream }}
      prod-frontend-upstream: ${{ inputs.prod-frontend-upstream }}
    secrets:
      PROXY_HOST: ${{ secrets.PROXY_HOST }}
      PROXY_USER: ${{ secrets.PROXY_USER }}
      PROXY_SSH_KEY: ${{ secrets.PROXY_SSH_KEY }}
      BASIC_AUTH_DEV_USER: ${{ secrets.BASIC_AUTH_DEV_USER }}
      BASIC_AUTH_DEV_HASH: ${{ secrets.BASIC_AUTH_DEV_HASH }}
      BASIC_AUTH_ADMIN_USER: ${{ secrets.BASIC_AUTH_ADMIN_USER }}
      BASIC_AUTH_ADMIN_HASH: ${{ secrets.BASIC_AUTH_ADMIN_HASH }}
